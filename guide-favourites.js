document.querySelectorAll('.favourite').forEach((button)=>{const key='tdfg:favourite:'+button.dataset.place;const sync=()=>{const active=localStorage.getItem(key)==='1';button.classList.toggle('active',active);button.setAttribute('aria-pressed',String(active));button.textContent=active?'♥ Saved':'♡ Add to Favourites'};sync();button.addEventListener('click',()=>{localStorage.setItem(key,localStorage.getItem(key)==='1'?'0':'1');sync()})});